add those files to the folder C:\10_Projects\Kafka\kafka\config 


once started, search for "from now on will use node" in the kafka log that is running in the terminal, this is the address you need to add to the app.py and plctokafka codes
